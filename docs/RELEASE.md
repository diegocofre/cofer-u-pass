# Release checklist

A stable release requires deterministic tests, adapter contract/fixture tests, controlled Chromium integration tests, migration tests, installed-wheel validation, Windows/Linux validation, documentation review, secret/evidence scanning, and supervised smoke tests for affected live providers. CI success alone is not sufficient publication approval.
